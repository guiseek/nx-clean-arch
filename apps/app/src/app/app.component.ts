import { Component } from '@angular/core'

@Component({
  selector: 'nx-clean-arch-root',
  template: '<router-outlet></router-outlet>',
  styleUrls: ['./app.component.scss'],
})
export class AppComponent {
  title = 'app'
}
