import { appAccountDataAccess } from './app-account-data-access'

describe('appAccountDataAccess', () => {
  it('should work', () => {
    expect(appAccountDataAccess()).toEqual('app-account-data-access')
  })
})
